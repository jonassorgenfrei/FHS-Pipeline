def helloCore(myParameter: str="Default") -> None:
    """A simple function in the core module.


    Args:
        myParameter (str, optional): MyDescription. Defaults to "Default".
    """
    print("Hello from core!")